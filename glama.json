{
  "name": "overte-mcp",
  "displayName": "Overte MCP Server",
  "description": "Model Context Protocol (MCP) server for Overte domain-server administration (real), with in-world entity spawning and JavaScript hot-reload scripting currently simulated pending a WebSocket bridge. Targets Overte, not current Vircadia (unrelated pivoted stack) -- see README.",
  "version": "0.1.0",
  "homepage": "https://github.com/sandraschi/overte-mcp",
  "repository": "https://github.com/sandraschi/overte-mcp.git",
  "license": "MIT",
  "author": "sandraschi",
  "categories": [
    "virtual-reality",
    "metaverse",
    "scripting",
    "automation"
  ],
  "tools": [
    {
      "name": "overte_domain_status",
      "description": "Retrieve connected-node telemetry and settings from an Overte Domain Server via /nodes.json and /settings.json. Real, unverified against a live server; falls back to labeled simulated data if unreachable.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "host": {
            "type": "string",
            "default": "localhost",
            "description": "Overte domain server host"
          },
          "port": {
            "type": "integer",
            "default": 40100,
            "description": "Overte domain administration port"
          },
          "username": {
            "type": "string",
            "description": "HTTP Basic Auth username for the domain-server admin API"
          },
          "password": {
            "type": "string",
            "description": "HTTP Basic Auth password for the domain-server admin API"
          }
        }
      }
    },
    {
      "name": "overte_entity_spawn",
      "description": "SIMULATED ONLY: no live Overte bridge exists yet. Returns fake spawn confirmation shaped like a real one, for tool/UI testing.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Name of the entity"
          },
          "type": {
            "type": "string",
            "default": "Box",
            "description": "Entity type: Box, Sphere, Web, Model"
          },
          "position": {
            "type": "array",
            "items": { "type": "number" },
            "default": [0, 0, 0],
            "description": "X, Y, Z translation coordinates"
          },
          "scale": {
            "type": "array",
            "items": { "type": "number" },
            "default": [1, 1, 1],
            "description": "X, Y, Z dimensions"
          },
          "model_url": {
            "type": "string",
            "description": "GLB/FBX model resource URL if type is Model"
          },
          "script_url": {
            "type": "string",
            "description": "Optional JavaScript behavior script URL to attach"
          }
        },
        "required": ["name"]
      }
    },
    {
      "name": "overte_script_inject",
      "description": "SIMULATED ONLY: no live Overte bridge exists yet. Returns fake script-update confirmation shaped like a real one, for tool/UI testing.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "entity_id": {
            "type": "string",
            "description": "Overte target entity UUID"
          },
          "script_url": {
            "type": "string",
            "description": "JavaScript behavior script URL"
          },
          "script_data": {
            "type": "object",
            "description": "Metadata parameters to inject into the script scope"
          }
        },
        "required": ["entity_id", "script_url"]
      }
    }
  ]
}
